# Skill: Chn2EngVideo_skill (YouTube 書摘影片全英文化自動生產線)

## 🎯 技能目的
此 Skill 旨在將一個已完成的**中文書摘影片素材目錄**，經過一系列自動化的英文化翻譯、配音生成、行銷素材翻譯、縮圖合成與自適應排版微調流程，自動且高效率地轉化為可用於製作**全英文 YouTube 影片**的高級影片素材（包含英文字幕綠幕軌、標準英文語音、多語氣 TTS 配音、英文縮圖、英文 SEO）。

---

## 📁 目錄與路徑結構慣例
假設來源書籍流水號為 `B134`，書籍目錄為 `134_20260527_你的生命是一場喜樂的量子遊戲`，此 Skill 在執行時將建立與預期以下目錄結構：

- **腳本英文化翻譯目錄**：自動將 `raw/txt134/`（中文逐句腳本）翻譯並存入新建立的 `raw/txt134_E/`。
- **英文語音目錄**：自動建立並存入 `raw/voice134_E/`，供全英文 TTS 使用。
- **中繼與行銷資料**：自動翻譯中文 `raw/info.txt` 並儲存為全英文 `raw/info_E.txt`。
- **英文縮圖與視覺素材**：
  * 優先尋找英文封面：`photo/英文封面.jpg` 或 `photo/原文封面.jpg`。
  * 備用中文封面：若英文封面不存在，則自動載入並使用 `photo/中文封面.jpg`。
  * 輸出路徑：自動生成英文版縮圖背景 `photo/縮圖背景_E.png` 與最終英文縮圖 `photo/youtube_thumbnail_E.png`。
- **綠幕排版測試截圖**：
  * 字幕測試圖：`photo/test_subtitle_render_1.png` ~ `3.png`。
  * 標題測試圖：`photo/test_title_render.png`。

---

## 🔄 執行步驟 (Step-by-Step SOP)

### Phase 1: 腳本英文化翻譯 (B134 Text Translator)
1. **[AI 執行] 批量逐句翻譯**：
   AI 掃描 `raw/txt{book_id}/` 下的所有中文腳本檔案，逐一翻譯為英文，並儲存到 `raw/txt{book_id}_E/` 中。
   * **⚠️ 核心規範：控制與標記符號 100% 保留**：
     * 句首角色符號必須完全對應保留：`。`（男主持 AA）、`。。`（女主持 BB）、`。。。`（專家 CC）。
     * 特殊前綴與分段標記必須原樣保留：`>>>>`（章節標題）、`@@@@`（過場黑畫面）、`!!!!`（隱藏字幕）。
     * 換行控制字元 `<` 必須予以保留，但英文語句若過長，則自動應用字詞分組換行。

---

### Phase 2: 英文語音合成 (TTS Engine Selection & Voice Generation)
2. **[AI 執行] 語音引擎配置**：
   使用 `edge-tts` 作為英文配音生成引擎，四個主持角色與專家的語音引擎配置標準如下：
   * **男主持人 (AA)**：`en-US-AndrewNeural`
   * **女主持人 (BB)**：`en-US-AriaNeural` （★ **特別規範：女主持人語速必須為標準自然語速 `+0%`，不進行任何加速**）。
   * **男受訪者/專家/作者 (CC - Male)**：`en-GB-RyanNeural`（高質感英式男音）。
   * **女受訪者/專家/作者 (CC - Female)**：`en-GB-SoniaNeural`（高質感英式女音）。
3. **[AI 執行] 批量配音生成**：
   執行配音生成程式（如 `generate_audio_eng.py`），將 `raw/txt{book_id}_E/` 下的英文逐句腳本全部生成為對應的 `.mp3` 音訊檔，存入 `raw/voice{book_id}_E/`。
   * 確保每一句音軌 1:1 對齊，空字串或過場字串（如 `@@@@`）自動生成靜音檔以確保時間軌正確。

---

### Phase 3: 英文 Meta 與 Premium 縮圖自動合成
4. **[AI 執行] 行銷資料翻譯**：
   AI 將 `raw/info.txt` 的所有行銷欄位翻譯為高點閱率的英文，存入 `raw/info_E.txt`：
   * 包含：英文頻道名 `HeyGo Library`、英文影片主標題、大小寫混合（Mixed-Case）的縮圖主標題（如 `"Your Thoughts Are Creating Reality - A Quantum Thinking Guide"`）、英文 SEO 標籤與章節時間軸說明。
   * **⚠️ 核心規範：【影片主標題】字數嚴格限制於 100 個半形字元以內**：
     * YouTube 規定影片主標題上限為 100 個字元（包含字母、空格與標點符號）。
     * 為了避免直接翻譯中文長標題導致英文長度溢出，**AI 必須在翻譯前，先對原始的中文標題進行裁切與精簡（僅保留最核心、最具張力的概念），然後才進行英文化翻譯**。翻譯後的英文主標題長度必須嚴格控制在 **100 個字元以內**！
   * ⏸️ **[等待審查]** 停下來讓使用者確認行銷內容與縮圖標題是否無誤。
5. **[AI 執行] 英文縮圖自動化合成**：
   * **A. 生成背景圖**：提取英文縮圖標題，使用 `generate_image` 繪圖工具自動生成高質感的 watercolor background 存為 `photo/縮圖背景_E.png`。
   * **B. 封面智能載入**：偵測英文/原文封面，若有則載入，否則自動降級使用中文封面。
   * **C. 自動執行合成**：執行縮圖合成程式（如 `generate_thumbnail_eng.py`），將英文主標題與副標題合成至背景圖上，輸出為 `photo/youtube_thumbnail_E.png`。

---

## Phase 4: 自適應英文字幕排版與綠幕渲染微調
6. **[AI 執行] 核心渲染器參數調整 (`--eng` 模式)**：
   主影片生成器 `BBD_video_generator_2026_eng.py` 在偵測到 `--eng` 參數時，自動套用以下英文高規格視覺排版設定：
   * **英文專用字型**：載入載入 Montserrat 系列字型（字幕使用 `Montserrat-Bold.ttf`）。
   * **字幕字體大小與安全邊界**：
     * 英文字幕字體設定為 **`40 pixels`**（中文為 64px）。
     * **字串寬度安全上限**：字幕限制在螢幕中央寬度 `1650px` 內。
   * **章節標題大小與對齊位置**：
     * 英文章節標題（Chapter Title）設定為 **`36 pixels`**。
     * **對齊位置**：放置在畫面**左上角** `(X = 50, Y = 50)` 座標。
     * **字串寬度安全上限**：標題限制在寬度 `1800px` 內。
7. **[AI 執行] 動態字詞動態寬度斷行演算法 (Pixel-Width Word Wrap)**：
   由於英文單字不能隨意切斷，排版程式必須使用以下演算法：
   * 不得僅依靠 `<` 強制換行。必須使用 PIL 的 `ImageDraw.textbbox` 動態測量渲染後的實際像素寬度。
   * 當單行像素寬度超過設定的寬度上限時，自動以**半形空白**為依據進行單字（Word）拆分並自動換行，防止首尾文字溢出螢幕邊緣或切斷。
8. **[AI 執行] 模擬渲染審查**：
   * 執行 `test_eng_subtitle.py`，取出腳本中最長的三個句子，進行 1920x1080 綠幕模擬渲染，生成 `photo/test_subtitle_render_1.png` ~ `3.png`。
   * 執行 `test_eng_title.py`，取出最長的章節標題（在 36px 下，最長標題寬度約為 `1652px`，可完美呈現在單行內不折行），進行 綠幕渲染，生成 `photo/test_title_render.png`。
   * ⏸️ **[等待審查]** 提供渲染結果圖連結，讓使用者審查英文字體、大小、斷行與左上角標題對齊位置。

---

## Phase 5: 就緒度檢查與最終渲染
9. **[AI 執行] 英文就緒度自動化檢查**：
   修改並執行 `check_readiness_eng.py`，程式會自動化完成 6 項檢查：
   - [ ] `info_E.txt` 結構與欄位完整性。
   - [ ] `youtube_thumbnail_E.png` 存在與大小。
   - [ ] `txt{book_id}_E/` 腳本與 `voice{book_id}_E/` 配音數量是否 1:1 完美匹配（均為 246 個）。
   - [ ] 主持人/專家頭像圖片 `AA.png`、`BB.png`、`CC.png` 存在性。
   - [ ] 41 張背景插圖（`0.png` ~ `40.png`）序號連續性與存在性。
   - [ ] 英文專用 Montserrat TTF 字型存在性。
   * 檢查通過即顯示 `✓ READY`。
10. **[人工/AI 執行] 最終全英文影片合成**：
    一切通過後，執行以下命令正式產出全英文複合綠幕影片素材：
    ```bash
    python3 BBD_video_generator_2026_eng.py --eng
    ```

---

## 💡 喚醒詞用法
之後當您需要對新的書籍目錄進行英文化轉換時，只需對我說：
`「啟動 Chn2EngVideo_skill，目標目錄是 [已完成中文書摘影片的目錄，如 134_20260527_你的生命是一場喜樂的量子遊戲]」`
我將會自動載入此 Skill 規範，引導並一鍵自動化為您完成整套英文化轉換流程！
